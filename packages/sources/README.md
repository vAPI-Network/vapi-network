# `@vapi-network/sources`

Discovery adapters for the vAPI registry, the x402 Bazaar, and local JSON files.
Results implement the `Source` and `Listing` contracts from `@vapi-network/core`
and retain their source provenance.

The x402scan adapter is intentionally a stub until x402scan documents a stable
public read API.
