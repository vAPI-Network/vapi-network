# @vapi-network/core

The non-custodial x402 protocol, local-wallet policy, discovery merge, network
guard, and append-only receipt primitives used by `vapi-network`.

Keys remain on the caller's machine. This package does not proxy payments or
send telemetry.
