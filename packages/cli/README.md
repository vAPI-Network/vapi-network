# @vapi-network/cli

The command-line interface used by the `vapi-network` distribution. It keeps
the wallet key encrypted on the local machine and signs x402 payments locally.

Most users should install or run the unscoped `vapi-network` package instead.
