## What & why

<!-- The problem and the intended outcome, in a paragraph. Link the issue. -->

## Changes

<!-- Everything this PR does, as a list. -->

## Checklist

- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pack:check` pass locally
- [ ] Tests added or updated for behaviour changes
- [ ] No runtime dependencies added to a published package (everything is bundled)
- [ ] Keys, payments, and telemetry stay on the user's machine (non-custodial invariant in AGENTS.md)
- [ ] Docs/README updated if the CLI, MCP tools, or config changed
- [ ] Commits are signed off (`git commit -s`, Developer Certificate of Origin)

## Breaking changes

<!-- "None" or what breaks and the migration. -->
